[project]
name = "blazingly-fast-python-ssg"
version = "0.1.0"
description = "DO NOT USE - Created for learning purposes only"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.8",
    "jinja2>=3.1.6",
    "list-all-files-recursively>=0.12",
    "markdown>=3.8",
    "pygments>=2.19.1",
    "python-frontmatter>=1.1.0",
]

[project.scripts]
foo = "ssg:main"
